language: ruby
rvm:
  - 1.9.3
env:
  - CI_REPORTS=shippable/testresults COVERAGE_REPORTS=shippable/codecoverage

services:
  - rabbitmq

notifications:
  email:
    test_ci2@yahoo.in
    
cache: true
